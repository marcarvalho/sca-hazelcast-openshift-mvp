apiVersion: v1
kind: Service
metadata:
  name: sca-api-service
  labels:
    app: sca-hazelcast-openshift-mvp
spec:
  type: ClusterIP
  selector:
    app: sca-hazelcast-openshift-mvp
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081